<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!--
        引入 Spring Boot 默认转换器定义（%clr / %wEx 等），
        Spring Boot 4.x 不再自动注册，必须显式 include。
    -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- ===================================================================
         属性定义
    =================================================================== -->
    <!--
         日志文件名不要用 <springProperty source="spring.application.name"/> 单独驱动：
         Logback 初始化往往早于 Environment 就绪，未解析时会出现 APP_NAME_IS_UNDEFINED.log。
         这里用 Logback 自带默认值；需要区分实例时在进程环境变量里设置 APP_NAME=xxx。
    -->
    <property name="APP_NAME" value="${APP_NAME:-aip-app}"/>
    <property name="LOG_HOME"    value="${LOG_HOME:-logs}"/>
    <property name="MAX_HISTORY" value="${LOG_MAX_HISTORY:-30}"/>
    <property name="MAX_SIZE"    value="${LOG_MAX_SIZE:-100MB}"/>
    <property name="TOTAL_CAP"   value="${LOG_TOTAL_CAP:-3GB}"/>

    <!-- ===================================================================
         日志格式
    =================================================================== -->
    <!-- 控制台：带颜色 -->
    <property name="CONSOLE_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx"/>

    <!-- 文件：纯文本，无颜色转义 -->
    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } --- [%t] %-50.50logger{49} : %m%n%wEx"/>

    <!-- ===================================================================
         Appender 1 - 控制台
    =================================================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ===================================================================
         Appender 2 - 滚动文件（全量，按天 + 大小双重触发）
         当天文件：logs/aip-app.log
         历史文件：logs/history/aip-app.yyyy-MM-dd.i.log
    =================================================================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}.log</file>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/history/${APP_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${MAX_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_CAP}</totalSizeCap>
            <cleanHistoryOnStart>false</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- ===================================================================
         Appender 3 - ERROR 单独文件（便于告警快速定位）
    =================================================================== -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/history/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${MAX_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ===================================================================
         异步包装（防止磁盘 IO 阻塞业务线程）
    =================================================================== -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>512</queueSize>
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNC_FILE_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>256</queueSize>
        <appender-ref ref="FILE_ERROR"/>
    </appender>

    <!-- ===================================================================
         第三方框架日志级别（减少噪音）
    =================================================================== -->
    <logger name="org.springframework"                         level="WARN"/>
    <logger name="org.springframework.boot"                    level="INFO"/>
    <logger name="org.springframework.boot.web"                level="INFO"/>
    <logger name="org.springframework.security"                level="WARN"/>
    <logger name="org.springframework.ai"                      level="INFO"/>
    <logger name="org.springframework.ai.observation"          level="WARN"/>
    <logger name="org.springframework.web.client.RestTemplate" level="INFO"/>
    <logger name="org.mybatis"                                 level="WARN"/>
    <logger name="com.baomidou.mybatisplus"                    level="WARN"/>
    <logger name="io.lettuce"                                  level="WARN"/>
    <logger name="io.netty"                                    level="WARN"/>

    <!-- ===================================================================
         Profile: dev / default（控制台 + 文件，业务代码 DEBUG）
    =================================================================== -->
    <springProfile name="dev,default">
        <logger name="com.sinosig.aip"                        level="DEBUG"/>
        <logger name="org.springframework.web.client.RestTemplate" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ASYNC_FILE_ERROR"/>
        </root>
    </springProfile>

    <!-- ===================================================================
         Profile: prod（只写文件，业务代码 INFO）
    =================================================================== -->
    <springProfile name="prod">
        <logger name="com.sinosig.aip" level="INFO"/>
        <root level="WARN">
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ASYNC_FILE_ERROR"/>
        </root>
    </springProfile>

    <!-- ===================================================================
         兜底 root（未匹配任何 Profile 时生效）
    =================================================================== -->
    <springProfile name="!dev &amp;&amp; !default &amp;&amp; !prod">
        <logger name="com.sinosig.aip" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ASYNC_FILE_ERROR"/>
        </root>
    </springProfile>

</configuration>
